<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>ROC DZ | PC Portables Pro & Gamers</title>
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@300;400;500;600;700;800;900&display=swap" rel="stylesheet">
    <style>
        * { margin: 0; padding: 0; box-sizing: border-box; }

        :root {
            --bg-deep: #0d0218;
            --bg-purple: #1a0a2e;
            --bg-card: rgba(255,255,255,0.03);
            --border: rgba(255,255,255,0.08);
            --pink: #e91e8c;
            --pink-light: #ff4da6;
            --purple: #b829dd;
            --purple-glow: rgba(184, 41, 221, 0.4);
            --text: #ffffff;
            --text-muted: rgba(255,255,255,0.5);
            --text-dim: rgba(255,255,255,0.3);
        }

        body {
            font-family: 'Inter', sans-serif;
            background: var(--bg-deep);
            color: var(--text);
            overflow-x: hidden;
        }

        #bg-canvas {
            position: fixed;
            top: 0; left: 0;
            width: 100%; height: 100%;
            z-index: 0;
            pointer-events: none;
        }

        .circuit-overlay {
            position: fixed;
            top: 0; left: 0;
            width: 100%; height: 100%;
            z-index: 1;
            pointer-events: none;
            opacity: 0.12;
            background-image: 
                linear-gradient(90deg, rgba(184,41,221,0.15) 1px, transparent 1px),
                linear-gradient(rgba(184,41,221,0.15) 1px, transparent 1px);
            background-size: 100px 100px;
        }

        .navbar {
            position: fixed;
            top: 0; left: 0; right: 0;
            z-index: 1000;
            padding: 20px 5%;
            display: flex;
            align-items: center;
            justify-content: space-between;
            background: linear-gradient(to bottom, rgba(13,2,24,0.95), transparent);
        }

        .nav-logo {
            font-size: 1.8rem;
            font-weight: 900;
            background: linear-gradient(135deg, var(--pink) 0%, var(--purple) 100%);
            -webkit-background-clip: text;
            -webkit-text-fill-color: transparent;
            letter-spacing: 2px;
        }

        .nav-logo span { font-weight: 300; }

        .nav-links {
            display: flex;
            gap: 3rem;
            list-style: none;
        }

        .nav-links a {
            color: var(--text);
            text-decoration: none;
            font-size: 0.95rem;
            font-weight: 500;
            position: relative;
            transition: color 0.3s;
        }

        .nav-links a::after {
            content: '';
            position: absolute;
            bottom: -5px; left: 0;
            width: 0; height: 2px;
            background: var(--pink);
            transition: width 0.3s;
        }

        .nav-links a:hover::after { width: 100%; }

        .nav-actions {
            display: flex;
            align-items: center;
            gap: 1.5rem;
        }

        .lang-switch {
            color: var(--text-muted);
            font-size: 0.8rem;
            font-weight: 600;
            cursor: pointer;
            display: flex;
            align-items: center;
            gap: 0.3rem;
        }

        .nav-icon {
            width: 22px; height: 22px;
            cursor: pointer;
            color: var(--text);
            transition: color 0.3s;
        }

        .nav-icon:hover { color: var(--pink); }

        .hero {
            position: relative;
            min-height: 100vh;
            display: flex;
            align-items: center;
            padding: 120px 5% 80px;
            z-index: 2;
            overflow: hidden;
        }

        .hero-content {
            max-width: 1400px;
            width: 100%;
            margin: 0 auto;
            display: grid;
            grid-template-columns: 1fr 1fr;
            gap: 4rem;
            align-items: center;
        }

        .hero-label {
            display: flex;
            align-items: center;
            gap: 1rem;
            margin-bottom: 2rem;
        }

        .hero-label .line {
            width: 40px; height: 1px;
            background: var(--pink);
        }

        .hero-label span {
            font-size: 0.75rem;
            font-weight: 700;
            letter-spacing: 3px;
            text-transform: uppercase;
            color: var(--pink-light);
        }

        .hero h1 {
            font-size: clamp(3rem, 6vw, 5.5rem);
            font-weight: 900;
            line-height: 1.05;
            letter-spacing: -2px;
            margin-bottom: 1.5rem;
        }

        .hero h1 .white { color: white; }
        .hero h1 .gradient {
            background: linear-gradient(135deg, var(--pink-light) 0%, var(--purple) 100%);
            -webkit-background-clip: text;
            -webkit-text-fill-color: transparent;
        }

        .hero-desc {
            color: var(--text-muted);
            font-size: 1rem;
            line-height: 1.8;
            max-width: 450px;
            margin-bottom: 2.5rem;
        }

        .btn-white {
            display: inline-flex;
            align-items: center;
            gap: 0.8rem;
            background: white;
            color: var(--bg-deep);
            padding: 1rem 2rem;
            border-radius: 50px;
            text-decoration: none;
            font-weight: 700;
            font-size: 0.9rem;
            border: none;
            cursor: pointer;
            transition: all 0.3s;
        }

        .btn-white:hover {
            transform: translateY(-3px);
            box-shadow: 0 10px 40px rgba(255,255,255,0.2);
        }

        .btn-white .arrow {
            width: 24px; height: 24px;
            background: var(--bg-deep);
            border-radius: 50%;
            display: flex;
            align-items: center;
            justify-content: center;
            color: white;
            font-size: 0.7rem;
        }

        .hero-image {
            position: relative;
            display: flex;
            justify-content: center;
            align-items: center;
        }

        .hero-image {
            position: relative;
            display: flex;
            justify-content: center;
            align-items: center;
            perspective: 1200px;
            transform-style: preserve-3d;
            min-height: 500px;
        }

        .hero-image img {
            max-width: 90%;
            max-height: 500px;
            object-fit: contain;
            filter: drop-shadow(0 30px 80px rgba(233, 30, 99, 0.4)) 
                    drop-shadow(0 10px 30px rgba(184, 41, 221, 0.3));
            animation: laptopFlyIn 2s cubic-bezier(0.23, 1, 0.32, 1) forwards,
                       laptopFloat 5s ease-in-out infinite 2s;
            transform-origin: center center;
            opacity: 0;
            position: relative;
            z-index: 5;
        }

        @keyframes laptopFlyIn {
            0% {
                opacity: 0;
                transform: translateX(300px) translateY(150px) rotateY(-55deg) rotateX(20deg) scale(0.6);
            }
            50% {
                opacity: 1;
                transform: translateX(-20px) translateY(-10px) rotateY(8deg) rotateX(-5deg) scale(1.05);
            }
            75% {
                transform: translateX(5px) translateY(3px) rotateY(-3deg) rotateX(2deg) scale(0.98);
            }
            100% {
                opacity: 1;
                transform: translateX(0) translateY(0) rotateY(-12deg) rotateX(5deg) scale(1);
            }
        }

        @keyframes laptopFloat {
            0%, 100% { 
                transform: translateY(0) rotateY(-12deg) rotateX(5deg) rotateZ(-2deg); 
            }
            20% { 
                transform: translateY(-18px) rotateY(-8deg) rotateX(8deg) rotateZ(1deg); 
            }
            40% { 
                transform: translateY(-25px) rotateY(-5deg) rotateX(3deg) rotateZ(-1deg); 
            }
            60% { 
                transform: translateY(-15px) rotateY(-10deg) rotateX(6deg) rotateZ(2deg); 
            }
            80% { 
                transform: translateY(-8px) rotateY(-14deg) rotateX(4deg) rotateZ(-1deg); 
            }
        }

        .hero-image::after {
            content: '';
            position: absolute;
            bottom: 20px;
            left: 50%;
            transform: translateX(-50%);
            width: 250px;
            height: 30px;
            background: radial-gradient(ellipse, rgba(233, 30, 99, 0.4) 0%, rgba(184, 41, 221, 0.2) 40%, transparent 70%);
            border-radius: 50%;
            filter: blur(15px);
            animation: shadowPulse 5s ease-in-out infinite;
            z-index: 4;
        }

        @keyframes shadowPulse {
            0%, 100% { transform: translateX(-50%) scale(1) rotateX(60deg); opacity: 0.6; }
            50% { transform: translateX(-50%) scale(0.7) rotateX(60deg); opacity: 0.3; }
        }

        .hero-image::before {
            content: '';
            position: absolute;
            top: 50%;
            left: 50%;
            transform: translate(-50%, -50%);
            width: 350px;
            height: 350px;
            background: radial-gradient(circle, rgba(233, 30, 140, 0.15) 0%, rgba(184, 41, 221, 0.1) 40%, transparent 70%);
            border-radius: 50%;
            filter: blur(60px);
            animation: glowPulse 4s ease-in-out infinite;
            z-index: 3;
        }

        .hero-glow {
            position: absolute;
            width: 600px; height: 600px;
            background: radial-gradient(circle, rgba(233, 30, 140, 0.25) 0%, rgba(184, 41, 221, 0.15) 40%, transparent 70%);
            border-radius: 50%;
            filter: blur(100px);
            animation: glowPulse 4s ease-in-out infinite;
            transform-origin: center;
        }

        @keyframes glowPulse {
            0%, 100% { transform: scale(1) rotate(0deg); opacity: 0.5; }
            33% { transform: scale(1.15) rotate(5deg); opacity: 0.8; }
            66% { transform: scale(1.05) rotate(-3deg); opacity: 0.6; }
        }

        .hero-glow-2 {
            position: absolute;
            width: 400px; height: 400px;
            background: radial-gradient(circle, rgba(184, 41, 221, 0.2) 0%, transparent 70%);
            border-radius: 50%;
            filter: blur(80px);
            animation: glowPulse2 5s ease-in-out infinite;
            top: -100px;
            right: -100px;
        }

        @keyframes glowPulse2 {
            0%, 100% { transform: scale(0.8); opacity: 0.4; }
            50% { transform: scale(1.2); opacity: 0.7; }
        }

        .scroll-indicator {
            position: absolute;
            right: 3%;
            bottom: 30%;
            display: flex;
            flex-direction: column;
            align-items: center;
            gap: 1rem;
            z-index: 10;
        }

        .scroll-indicator span {
            writing-mode: vertical-rl;
            font-size: 0.65rem;
            letter-spacing: 2px;
            text-transform: uppercase;
            color: var(--text-dim);
        }

        .scroll-line {
            width: 1px;
            height: 60px;
            background: linear-gradient(to bottom, var(--pink), transparent);
            animation: scrollLine 2s ease-in-out infinite;
        }

        @keyframes scrollLine {
            0% { transform: scaleY(0); transform-origin: top; }
            50% { transform: scaleY(1); transform-origin: top; }
            50.1% { transform-origin: bottom; }
            100% { transform: scaleY(0); transform-origin: bottom; }
        }

        .brand-marquee {
            position: absolute;
            bottom: 40px;
            left: 0; right: 0;
            overflow: hidden;
            opacity: 0.4;
        }

        .marquee-track {
            display: flex;
            gap: 4rem;
            animation: marquee 20s linear infinite;
            width: max-content;
        }

        @keyframes marquee {
            0% { transform: translateX(0); }
            100% { transform: translateX(-50%); }
        }

        .brand-item {
            font-size: 1rem;
            font-weight: 700;
            color: var(--text-dim);
            text-transform: uppercase;
            letter-spacing: 2px;
            white-space: nowrap;
        }

        section { position: relative; z-index: 2; }

        .section-label {
            text-align: center;
            font-size: 0.75rem;
            font-weight: 700;
            letter-spacing: 4px;
            text-transform: uppercase;
            color: var(--text-dim);
            margin-bottom: 1rem;
        }

        .section-title {
            text-align: center;
            font-size: clamp(2rem, 4vw, 3.5rem);
            font-weight: 900;
            margin-bottom: 3rem;
        }

        .section-title .gradient {
            background: linear-gradient(135deg, var(--pink-light) 0%, var(--purple) 100%);
            -webkit-background-clip: text;
            -webkit-text-fill-color: transparent;
        }

        .categories {
            padding: 80px 0;
            overflow: hidden;
        }

        .category-header {
            text-align: center;
            margin-bottom: 3rem;
        }

        .category-header h2 {
            font-size: clamp(2rem, 4vw, 3.5rem);
            font-weight: 900;
            line-height: 1.2;
        }

        .category-header h2 .gradient {
            background: linear-gradient(135deg, var(--pink-light) 0%, var(--purple) 100%);
            -webkit-background-clip: text;
            -webkit-text-fill-color: transparent;
        }

        .category-subtitle {
            font-size: 1.2rem;
            font-weight: 800;
            text-transform: uppercase;
            letter-spacing: 3px;
            text-align: center;
            margin-bottom: 0.5rem;
        }

        .category-subtitle-desc {
            text-align: center;
            color: var(--text-muted);
            font-size: 0.9rem;
            margin-bottom: 2rem;
        }

        .carousel-container {
            position: relative;
            padding: 0 5%;
        }

        .carousel {
            display: flex;
            gap: 1.5rem;
            overflow-x: auto;
            scroll-snap-type: x mandatory;
            scroll-behavior: smooth;
            padding: 1rem 0 2rem;
            -ms-overflow-style: none;
            scrollbar-width: none;
        }

        .carousel::-webkit-scrollbar { display: none; }

        .carousel-card {
            flex: 0 0 280px;
            scroll-snap-align: start;
            background: var(--bg-card);
            border: 1px solid var(--border);
            border-radius: 20px;
            overflow: hidden;
            cursor: pointer;
            transition: all 0.4s;
            position: relative;
        }

        .carousel-card::before {
            content: '';
            position: absolute;
            bottom: 0; left: 0; right: 0;
            height: 3px;
            background: linear-gradient(90deg, var(--pink), var(--purple));
            transform: scaleX(0);
            transition: transform 0.4s;
        }

        .carousel-card:hover {
            transform: translateY(-10px);
            border-color: rgba(255,255,255,0.15);
            box-shadow: 0 20px 60px rgba(184, 41, 221, 0.15);
        }

        .carousel-card:hover::before { transform: scaleX(1); }

        .carousel-card img {
            width: 100%;
            height: 200px;
            object-fit: cover;
            display: block;
        }

        .carousel-card-body {
            padding: 1.5rem;
            text-align: center;
        }

        .card-tag {
            display: inline-flex;
            align-items: center;
            gap: 0.4rem;
            font-size: 0.65rem;
            font-weight: 700;
            text-transform: uppercase;
            letter-spacing: 1px;
            margin-bottom: 0.8rem;
        }

        .card-tag .dot {
            width: 6px; height: 6px;
            border-radius: 50%;
        }

        .tag-fiabilite { color: #4fc3f7; }
        .tag-fiabilite .dot { background: #4fc3f7; }
        .tag-pro { color: #29b6f6; }
        .tag-pro .dot { background: #29b6f6; }
        .tag-performance { color: #ff7043; }
        .tag-performance .dot { background: #ff7043; }
        .tag-gaming { color: #e91e8c; }
        .tag-gaming .dot { background: #e91e8c; }
        .tag-polyvalent { color: #66bb6a; }
        .tag-polyvalent .dot { background: #66bb6a; }
        .tag-premium { color: #ab47bc; }
        .tag-premium .dot { background: #ab47bc; }
        .tag-saisie { color: #ce93d8; }
        .tag-saisie .dot { background: #ce93d8; }
        .tag-precision { color: #f48fb1; }
        .tag-precision .dot { background: #f48fb1; }
        .tag-audio { color: #ffca28; }
        .tag-audio .dot { background: #ffca28; }
        .tag-display { color: #26c6da; }
        .tag-display .dot { background: #26c6da; }
        .tag-video { color: #42a5f5; }
        .tag-video .dot { background: #42a5f5; }
        .tag-transport { color: #ff8a65; }
        .tag-transport .dot { background: #ff8a65; }
        .tag-energie { color: #d4e157; }
        .tag-energie .dot { background: #d4e157; }
        .tag-connectique { color: #ba68c8; }
        .tag-connectique .dot { background: #ba68c8; }
        .tag-liaison { color: #4dd0e1; }
        .tag-liaison .dot { background: #4dd0e1; }
        .tag-ergonomie { color: #90caf9; }
        .tag-ergonomie .dot { background: #90caf9; }
        .tag-memoire { color: #80cbc4; }
        .tag-memoire .dot { background: #80cbc4; }

        .carousel-card h3 {
            font-size: 1.3rem;
            font-weight: 800;
            margin-bottom: 0.8rem;
        }

        .carousel-card .explore {
            font-size: 0.7rem;
            font-weight: 700;
            text-transform: uppercase;
            letter-spacing: 1px;
            color: var(--text-dim);
            display: flex;
            align-items: center;
            justify-content: center;
            gap: 0.5rem;
            transition: color 0.3s;
        }

        .carousel-card:hover .explore { color: var(--pink); }

        .carousel-nav-btn {
            position: absolute;
            top: 50%;
            transform: translateY(-50%);
            width: 45px; height: 45px;
            background: rgba(255,255,255,0.05);
            border: 1px solid var(--border);
            border-radius: 50%;
            display: flex;
            align-items: center;
            justify-content: center;
            cursor: pointer;
            color: var(--text);
            font-size: 1.2rem;
            transition: all 0.3s;
            z-index: 10;
        }

        .carousel-nav-btn:hover {
            background: var(--pink);
            border-color: var(--pink);
        }

        .carousel-nav-btn.prev { left: 2%; }
        .carousel-nav-btn.next { right: 2%; }

        .looking-for {
            padding: 100px 5%;
        }

        .looking-grid {
            display: flex;
            justify-content: center;
            gap: 3rem;
            max-width: 1000px;
            margin: 0 auto;
            perspective: 1000px;
        }

        .tilt-card {
            flex: 1;
            max-width: 450px;
            background: var(--bg-card);
            border: 1px solid var(--border);
            border-radius: 24px;
            overflow: hidden;
            position: relative;
            transition: all 0.5s;
        }

        .tilt-card:nth-child(1) { transform: rotate(-3deg); }
        .tilt-card:nth-child(2) { transform: rotate(3deg); margin-top: 2rem; }

        .tilt-card:hover {
            transform: rotate(0deg) translateY(-10px);
            border-color: rgba(255,255,255,0.15);
            box-shadow: 0 30px 60px rgba(0,0,0,0.4);
        }

        .tilt-card .badge {
            position: absolute;
            top: 1.5rem; left: 1.5rem;
            padding: 0.4rem 1rem;
            border-radius: 4px;
            font-size: 0.7rem;
            font-weight: 800;
            text-transform: uppercase;
            z-index: 5;
        }

        .badge-new { background: #00c853; color: white; }
        .badge-refurb { background: #ff9100; color: white; }

        .tilt-card img {
            width: 100%;
            height: 280px;
            object-fit: cover;
            display: block;
        }

        .tilt-card-body {
            padding: 2rem;
        }

        .tilt-card-body h3 {
            font-size: 1.8rem;
            font-weight: 900;
            margin-bottom: 0.5rem;
        }

        .tilt-card-body .sub {
            color: var(--text-muted);
            font-size: 0.85rem;
            margin-bottom: 1rem;
        }

        .tilt-card-body p {
            color: var(--text-dim);
            font-size: 0.85rem;
            line-height: 1.7;
            margin-bottom: 1.5rem;
        }

        .tilt-card-body .link {
            display: flex;
            align-items: center;
            gap: 0.5rem;
            color: var(--text);
            text-decoration: none;
            font-size: 0.85rem;
            font-weight: 700;
            transition: color 0.3s;
        }

        .tilt-card-body .link:hover { color: var(--pink); }

        .tilt-card-body .link .arrow-circle {
            width: 36px; height: 36px;
            border: 1px solid var(--border);
            border-radius: 50%;
            display: flex;
            align-items: center;
            justify-content: center;
            margin-left: auto;
            transition: all 0.3s;
        }

        .tilt-card:hover .link .arrow-circle {
            background: var(--pink);
            border-color: var(--pink);
        }

        .pick-day {
            padding: 100px 5%;
            position: relative;
        }

        .pick-container {
            max-width: 1300px;
            margin: 0 auto;
            display: grid;
            grid-template-columns: 1fr 1.2fr 1fr;
            gap: 3rem;
            align-items: center;
        }

        .pick-title-stack { position: relative; }

        .pick-title-stack .label {
            font-size: 0.7rem;
            font-weight: 700;
            letter-spacing: 3px;
            text-transform: uppercase;
            color: var(--pink);
            margin-bottom: 1rem;
        }

        .pick-title-stack h2 {
            font-size: clamp(2.5rem, 5vw, 4.5rem);
            font-weight: 900;
            line-height: 1;
            letter-spacing: -2px;
        }

        .pick-title-stack h2 .white { color: white; }
        .pick-title-stack h2 .gradient {
            background: linear-gradient(135deg, var(--pink-light) 0%, var(--purple) 100%);
            -webkit-background-clip: text;
            -webkit-text-fill-color: transparent;
        }

        .pick-specs { margin-top: 2rem; }

        .spec-row {
            display: flex;
            justify-content: space-between;
            align-items: center;
            padding: 1rem 0;
            border-bottom: 1px solid var(--border);
            gap: 1rem;
        }

        .spec-row .label {
            font-size: 0.7rem;
            font-weight: 700;
            text-transform: uppercase;
            letter-spacing: 1px;
            color: var(--text-dim);
            white-space: nowrap;
        }

        .spec-row .value {
            font-size: 0.75rem;
            font-weight: 600;
            color: var(--pink-light);
            text-align: right;
            line-height: 1.5;
        }

        .pick-image {
            position: relative;
            display: flex;
            justify-content: center;
        }

        .pick-image img {
            max-width: 100%;
            border-radius: 16px;
            box-shadow: 0 30px 80px rgba(0,0,0,0.5);
        }

        .pick-image::before {
            content: '';
            position: absolute;
            top: 50%; left: 50%;
            transform: translate(-50%, -50%);
            width: 300px; height: 300px;
            background: radial-gradient(circle, var(--purple-glow) 0%, transparent 70%);
            border-radius: 50%;
            filter: blur(40px);
            z-index: -1;
        }

        .pick-info { text-align: right; }

        .pick-info .status-badge {
            display: inline-block;
            padding: 0.3rem 1rem;
            background: rgba(233, 30, 99, 0.1);
            border: 1px solid rgba(233, 30, 99, 0.3);
            border-radius: 50px;
            font-size: 0.7rem;
            font-weight: 700;
            color: var(--pink);
            margin-bottom: 1rem;
        }

        .pick-info .price-label {
            font-size: 0.75rem;
            color: var(--text-dim);
            text-transform: uppercase;
            letter-spacing: 2px;
            margin-bottom: 0.5rem;
        }

        .pick-info .price {
            font-size: 3rem;
            font-weight: 900;
            color: var(--pink);
            line-height: 1;
            margin-bottom: 0.5rem;
        }

        .pick-info .price span {
            font-size: 1rem;
            color: var(--text-muted);
        }

        .pick-info .stock {
            font-size: 0.8rem;
            color: var(--text-dim);
            text-transform: uppercase;
            letter-spacing: 2px;
            margin-bottom: 2rem;
        }

        .pick-info .btn-product {
            display: inline-flex;
            align-items: center;
            gap: 1rem;
            background: white;
            color: var(--bg-deep);
            padding: 1rem 2.5rem;
            border-radius: 4px;
            text-decoration: none;
            font-weight: 800;
            font-size: 0.85rem;
            text-transform: uppercase;
            letter-spacing: 1px;
            margin-bottom: 2rem;
            transition: all 0.3s;
        }

        .pick-info .btn-product:hover {
            transform: translateX(-5px);
            box-shadow: 0 10px 40px rgba(255,255,255,0.2);
        }

        .pick-trust { text-align: right; }
        .pick-trust p {
            font-size: 0.7rem;
            color: var(--text-dim);
            margin-bottom: 0.3rem;
            font-weight: 600;
            letter-spacing: 1px;
        }

        .why-us {
            padding: 100px 5%;
        }

        .glass-container {
            max-width: 1200px;
            margin: 0 auto;
            background: rgba(255,255,255,0.02);
            backdrop-filter: blur(20px);
            border: 1px solid rgba(255,255,255,0.08);
            border-radius: 40px;
            padding: 4rem;
            position: relative;
            overflow: hidden;
        }

        .glass-container::before {
            content: '';
            position: absolute;
            top: -200px; right: -200px;
            width: 500px; height: 500px;
            background: radial-gradient(circle, var(--purple-glow) 0%, transparent 70%);
            border-radius: 50%;
            filter: blur(60px);
            opacity: 0.6;
        }

        .glass-header {
            display: flex;
            justify-content: space-between;
            align-items: flex-start;
            margin-bottom: 3rem;
            position: relative;
            z-index: 1;
        }

        .glass-header .left .label {
            display: flex;
            align-items: center;
            gap: 1rem;
            font-size: 0.75rem;
            font-weight: 700;
            letter-spacing: 3px;
            text-transform: uppercase;
            color: var(--text-dim);
            margin-bottom: 1rem;
        }

        .glass-header .left .label .line {
            width: 30px; height: 1px;
            background: var(--text-dim);
        }

        .glass-header h2 {
            font-size: clamp(2rem, 3vw, 2.8rem);
            font-weight: 900;
            line-height: 1.2;
        }

        .glass-header h2 .gradient {
            background: linear-gradient(135deg, var(--pink-light) 0%, var(--purple) 100%);
            -webkit-background-clip: text;
            -webkit-text-fill-color: transparent;
        }

        .glass-header .right {
            max-width: 300px;
            text-align: right;
            color: var(--text-muted);
            font-size: 0.9rem;
            line-height: 1.7;
        }

        .glass-grid {
            display: grid;
            grid-template-columns: repeat(4, 1fr);
            gap: 1.5rem;
            position: relative;
            z-index: 1;
        }

        .glass-card {
            background: rgba(255,255,255,0.03);
            border: 1px solid var(--border);
            border-radius: 20px;
            padding: 2rem;
            position: relative;
            overflow: hidden;
            transition: all 0.4s;
        }

        .glass-card::before {
            content: '';
            position: absolute;
            top: 0; left: 0; right: 0;
            height: 2px;
            background: linear-gradient(90deg, var(--pink), var(--purple));
            transform: scaleX(0);
            transition: transform 0.4s;
        }

        .glass-card:hover {
            transform: translateY(-5px);
            border-color: rgba(255,255,255,0.15);
        }

        .glass-card:hover::before { transform: scaleX(1); }

        .glass-card .bg-num {
            position: absolute;
            bottom: -10px; right: 10px;
            font-size: 5rem;
            font-weight: 900;
            color: rgba(255,255,255,0.03);
            line-height: 1;
            pointer-events: none;
        }

        .glass-card .icons {
            display: flex;
            justify-content: space-between;
            margin-bottom: 1.5rem;
        }

        .glass-card .icons svg {
            width: 28px; height: 28px;
            color: var(--text-dim);
        }

        .glass-card h3 {
            font-size: 1.1rem;
            font-weight: 800;
            margin-bottom: 0.8rem;
            line-height: 1.3;
        }

        .glass-card p {
            font-size: 0.8rem;
            color: var(--text-muted);
            line-height: 1.7;
        }

        .back-to-top {
            position: fixed;
            bottom: 2rem; right: 2rem;
            width: 50px; height: 50px;
            background: var(--pink);
            border-radius: 50%;
            display: flex;
            align-items: center;
            justify-content: center;
            cursor: pointer;
            z-index: 999;
            border: none;
            color: white;
            font-size: 1.2rem;
            box-shadow: 0 5px 20px rgba(233, 30, 99, 0.4);
            transition: all 0.3s;
            opacity: 0;
            visibility: hidden;
        }

        .back-to-top.visible {
            opacity: 1;
            visibility: visible;
        }

        .back-to-top:hover {
            transform: translateY(-5px);
            box-shadow: 0 10px 30px rgba(233, 30, 99, 0.6);
        }

        .fade-up {
            opacity: 0;
            transform: translateY(40px);
            transition: all 0.8s cubic-bezier(0.16, 1, 0.3, 1);
        }

        .fade-up.visible {
            opacity: 1;
            transform: translateY(0);
        }

        .fade-left {
            opacity: 0;
            transform: translateX(-40px);
            transition: all 0.8s cubic-bezier(0.16, 1, 0.3, 1);
        }

        .fade-left.visible {
            opacity: 1;
            transform: translateX(0);
        }

        .fade-right {
            opacity: 0;
            transform: translateX(40px);
            transition: all 0.8s cubic-bezier(0.16, 1, 0.3, 1);
        }

        .fade-right.visible {
            opacity: 1;
            transform: translateX(0);
        }

        .scale-in {
            opacity: 0;
            transform: scale(0.9);
            transition: all 0.8s cubic-bezier(0.16, 1, 0.3, 1);
        }

        .scale-in.visible {
            opacity: 1;
            transform: scale(1);
        }

        @media (max-width: 1024px) {
            .hero-content { grid-template-columns: 1fr; text-align: center; }
            .hero-desc { margin-left: auto; margin-right: auto; }
            .hero-image { order: -1; }
            .pick-container { grid-template-columns: 1fr; text-align: center; }
            .pick-info { text-align: center; }
            .pick-trust { text-align: center; }
            .glass-grid { grid-template-columns: repeat(2, 1fr); }
            .glass-header { flex-direction: column; gap: 1rem; }
            .glass-header .right { text-align: left; }
            .looking-grid { flex-direction: column; align-items: center; }
            .tilt-card:nth-child(2) { margin-top: 0; }
        }

        @media (max-width: 768px) {
            .nav-links { display: none; }
            .glass-grid { grid-template-columns: 1fr; }
            .scroll-indicator { display: none; }
        }
    </style>
<base target="_blank">
</head>
<body>
    <canvas id="bg-canvas"></canvas>
    <div class="circuit-overlay"></div>

    <nav class="navbar">
        <div class="nav-logo">R<span>⊙</span>C DZ</div>
        <ul class="nav-links">
            <li><a href="#hero">Home</a></li>
            <li><a href="#categories">Shop</a></li>
            <li><a href="#why-us">About</a></li>
        </ul>
        <div class="nav-actions">
            <svg class="nav-icon" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M21 21l-6-6m2-5a7 7 0 11-14 0 7 7 0 0114 0z"/>
            </svg>
            <div class="lang-switch">
                <svg width="16" height="16" fill="currentColor" viewBox="0 0 24 24"><path d="M12 2C6.48 2 2 6.48 2 12s4.48 10 10 10 10-4.48 10-10S17.52 2 12 2zm-1 17.93c-3.95-.49-7-3.85-7-7.93 0-.62.08-1.21.21-1.79L9 15v1c0 1.1.9 2 2 2v1.93zm6.9-2.54c-.26-.81-1-1.39-1.9-1.39h-1v-3c0-.55-.45-1-1-1H8v-2h2c.55 0 1-.45 1-1V7h2c1.1 0 2-.9 2-2v-.41c2.93 1.19 5 4.06 5 7.41 0 2.08-.8 3.97-2.1 5.39z"/></svg>
                US EN
                <svg width="12" height="12" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M19 9l-7 7-7-7"/></svg>
            </div>
            <svg class="nav-icon" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M13 16V6a1 1 0 00-1-1H4a1 1 0 00-1 1v10a1 1 0 001 1h1m8-1a1 1 0 01-1 1H9m4-1V8a1 1 0 011-1h2.586a1 1 0 01.707.293l3.414 3.414a1 1 0 01.293.707V16a1 1 0 01-1 1h-1m-6-1a1 1 0 001 1h1M5 17a2 2 0 104 0m-4 0a2 2 0 114 0m6 0a2 2 0 104 0m-4 0a2 2 0 114 0"/>
            </svg>
            <svg class="nav-icon" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M16 11V7a4 4 0 00-8 0v4M5 9h14l1 12H4L5 9z"/>
            </svg>
            <svg class="nav-icon" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M16 7a4 4 0 11-8 0 4 4 0 018 0zM12 14a7 7 0 00-7 7h14a7 7 0 00-7-7z"/>
            </svg>
        </div>
    </nav>

    <section class="hero" id="hero">
        <div class="hero-content">
            <div class="hero-text fade-up">
                <div class="hero-label">
                    <div class="line"></div>
                    <span>Republic of Computer</span>
                </div>
                <h1>
                    <span class="white">The Elite of</span><br>
                    <span class="gradient">Laptops.</span>
                </h1>
                <p class="hero-desc">Discover our selection of laptops (new and pre-owned) tested and guaranteed, with premium accessories. Delivery available across all 69 wilayas.</p>
                <a href="#categories" class="btn-white">
                    View models
                    <span class="arrow">→</span>
                </a>
            </div>
            <div class="hero-image fade-right">
                <div class="hero-glow"></div>
                <div class="hero-glow-2"></div>
                <img src="/laptop-nobg.png" alt="Laptop">
            </div>
        </div>

        <div class="scroll-indicator">
            <span>Scroll to explore</span>
            <div class="scroll-line"></div>
        </div>

        <div class="brand-marquee">
            <div class="marquee-track">
                <span class="brand-item">DELL</span>
                <span class="brand-item">HP</span>
                <span class="brand-item">LENOVO</span>
                <span class="brand-item">ASUS</span>
                <span class="brand-item">MSI</span>
                <span class="brand-item">APPLE</span>
                <span class="brand-item">ACER</span>
                <span class="brand-item">DELL</span>
                <span class="brand-item">HP</span>
                <span class="brand-item">LENOVO</span>
                <span class="brand-item">ASUS</span>
                <span class="brand-item">MSI</span>
                <span class="brand-item">APPLE</span>
                <span class="brand-item">ACER</span>
                <span class="brand-item">DELL</span>
                <span class="brand-item">HP</span>
                <span class="brand-item">LENOVO</span>
                <span class="brand-item">ASUS</span>
                <span class="brand-item">MSI</span>
                <span class="brand-item">APPLE</span>
                <span class="brand-item">ACER</span>
            </div>
        </div>
    </section>

    <section class="categories" id="categories">
        <div class="category-header">
            <div class="section-label">Categories</div>
            <h2>Explore our<br><span class="gradient">collections.</span></h2>
        </div>

        <div class="category-subtitle">Laptop</div>
        <div class="category-subtitle-desc">Available brands</div>

        <div class="carousel-container">
            <button class="carousel-nav-btn prev" onclick="scrollCarousel('laptopCarousel', -300)">‹</button>
            <div class="carousel" id="laptopCarousel">
                <div class="carousel-card">
                    <img src="https://images.unsplash.com/photo-1588872657578-7efd1f1555ed?w=400&q=80" alt="Dell">
                    <div class="carousel-card-body">
                        <div class="card-tag tag-fiabilite"><span class="dot"></span>Fiabilité</div>
                        <h3>Dell</h3>
                        <div class="explore">Explore Collection →</div>
                    </div>
                </div>
                <div class="carousel-card">
                    <img src="https://images.unsplash.com/photo-1544731612-de7f96afe55f?w=400&q=80" alt="HP">
                    <div class="carousel-card-body">
                        <div class="card-tag tag-pro"><span class="dot"></span>Pro</div>
                        <h3>HP</h3>
                        <div class="explore">Explore Collection →</div>
                    </div>
                </div>
                <div class="carousel-card">
                    <img src="https://images.unsplash.com/photo-1527434065213-849f5e9607ea?w=400&q=80" alt="Lenovo">
                    <div class="carousel-card-body">
                        <div class="card-tag tag-performance"><span class="dot"></span>Performance</div>
                        <h3>Lenovo</h3>
                        <div class="explore">Explore Collection →</div>
                    </div>
                </div>
                <div class="carousel-card">
                    <img src="https://images.unsplash.com/photo-1603302576837-37561b2e2302?w=400&q=80" alt="ASUS">
                    <div class="carousel-card-body">
                        <div class="card-tag tag-gaming"><span class="dot"></span>Gaming</div>
                        <h3>ASUS</h3>
                        <div class="explore">Explore Collection →</div>
                    </div>
                </div>
                <div class="carousel-card">
                    <img src="https://images.unsplash.com/photo-1587829741301-dc798b83add3?w=400&q=80" alt="Acer">
                    <div class="carousel-card-body">
                        <div class="card-tag tag-polyvalent"><span class="dot"></span>Polyvalent</div>
                        <h3>Acer</h3>
                        <div class="explore">Explore Collection →</div>
                    </div>
                </div>
                <div class="carousel-card">
                    <img src="https://images.unsplash.com/photo-1593640408182-31c70c8268f5?w=400&q=80" alt="MSI">
                    <div class="carousel-card-body">
                        <div class="card-tag tag-gaming"><span class="dot"></span>Gaming</div>
                        <h3>MSI</h3>
                        <div class="explore">Explore Collection →</div>
                    </div>
                </div>
                <div class="carousel-card">
                    <img src="https://images.unsplash.com/photo-1611186871348-b1ce696e52c9?w=400&q=80" alt="Apple">
                    <div class="carousel-card-body">
                        <div class="card-tag tag-premium"><span class="dot"></span>Premium</div>
                        <h3>Apple</h3>
                        <div class="explore">Explore Collection →</div>
                    </div>
                </div>
            </div>
            <button class="carousel-nav-btn next" onclick="scrollCarousel('laptopCarousel', 300)">›</button>
        </div>

        <div style="margin-top: 4rem;">
            <div class="category-subtitle">Accessories</div>
            <div class="category-subtitle-desc">Equip your setup</div>

            <div class="carousel-container">
                <button class="carousel-nav-btn prev" onclick="scrollCarousel('accCarousel', -300)">‹</button>
                <div class="carousel" id="accCarousel">
                    <div class="carousel-card">
                        <img src="https://images.unsplash.com/photo-1587829741301-dc798b83add3?w=400&q=80" alt="Keyboards">
                        <div class="carousel-card-body">
                            <div class="card-tag tag-saisie"><span class="dot"></span>Saisie</div>
                            <h3>Keyboards</h3>
                            <div class="explore">Explore Collection →</div>
                        </div>
                    </div>
                    <div class="carousel-card">
                        <img src="https://images.unsplash.com/photo-1527864550417-7fd91fc51a46?w=400&q=80" alt="Mice">
                        <div class="carousel-card-body">
                            <div class="card-tag tag-precision"><span class="dot"></span>Précision</div>
                            <h3>Mice</h3>
                            <div class="explore">Explore Collection →</div>
                        </div>
                    </div>
                    <div class="carousel-card">
                        <img src="https://images.unsplash.com/photo-1505740420928-5e560c06d30e?w=400&q=80" alt="Headsets">
                        <div class="carousel-card-body">
                            <div class="card-tag tag-audio"><span class="dot"></span>Audio</div>
                            <h3>Headsets</h3>
                            <div class="explore">Explore Collection →</div>
                        </div>
                    </div>
                    <div class="carousel-card">
                        <img src="https://images.unsplash.com/photo-1527443224154-c4a3942d3acf?w=400&q=80" alt="Monitors">
                        <div class="carousel-card-body">
                            <div class="card-tag tag-display"><span class="dot"></span>Display</div>
                            <h3>Monitors</h3>
                            <div class="explore">Explore Collection →</div>
                        </div>
                    </div>
                    <div class="carousel-card">
                        <img src="https://images.unsplash.com/photo-1587829741301-dc798b83add3?w=400&q=80" alt="Webcams">
                        <div class="carousel-card-body">
                            <div class="card-tag tag-video"><span class="dot"></span>Vidéo</div>
                            <h3>Webcams</h3>
                            <div class="explore">Explore Collection →</div>
                        </div>
                    </div>
                    <div class="carousel-card">
                        <img src="https://images.unsplash.com/photo-1592840496694-26d035b52b48?w=400&q=80" alt="Controllers">
                        <div class="carousel-card-body">
                            <div class="card-tag tag-gaming"><span class="dot"></span>Gaming</div>
                            <h3>Controllers</h3>
                            <div class="explore">Explore Collection →</div>
                        </div>
                    </div>
                    <div class="carousel-card">
                        <img src="https://images.unsplash.com/photo-1553062407-98eeb64c6a62?w=400&q=80" alt="Bags">
                        <div class="carousel-card-body">
                            <div class="card-tag tag-transport"><span class="dot"></span>Transport</div>
                            <h3>Bags</h3>
                            <div class="explore">Explore Collection →</div>
                        </div>
                    </div>
                    <div class="carousel-card">
                        <img src="https://images.unsplash.com/photo-1583863788434-e58a36330cf0?w=400&q=80" alt="Chargers">
                        <div class="carousel-card-body">
                            <div class="card-tag tag-energie"><span class="dot"></span>Énergie</div>
                            <h3>Chargers</h3>
                            <div class="explore">Explore Collection →</div>
                        </div>
                    </div>
                    <div class="carousel-card">
                        <img src="https://images.unsplash.com/photo-1625766763788-95dcce9bf5ac?w=400&q=80" alt="Hubs">
                        <div class="carousel-card-body">
                            <div class="card-tag tag-connectique"><span class="dot"></span>Connectique</div>
                            <h3>Hubs & Adapters</h3>
                            <div class="explore">Explore Collection →</div>
                        </div>
                    </div>
                    <div class="carousel-card">
                        <img src="https://images.unsplash.com/photo-1558618666-fcd25c85f82e?w=400&q=80" alt="Cables">
                        <div class="carousel-card-body">
                            <div class="card-tag tag-liaison"><span class="dot"></span>Liaison</div>
                            <h3>Cables</h3>
                            <div class="explore">Explore Collection →</div>
                        </div>
                    </div>
                    <div class="carousel-card">
                        <img src="https://images.unsplash.com/photo-1611186871348-b1ce696e52c9?w=400&q=80" alt="Stands">
                        <div class="carousel-card-body">
                            <div class="card-tag tag-ergonomie"><span class="dot"></span>Ergonomie</div>
                            <h3>Stands</h3>
                            <div class="explore">Explore Collection →</div>
                        </div>
                    </div>
                    <div class="carousel-card">
                        <img src="https://images.unsplash.com/photo-1597872252165-4827a235d7bc?w=400&q=80" alt="Storage">
                        <div class="carousel-card-body">
                            <div class="card-tag tag-memoire"><span class="dot"></span>Mémoire</div>
                            <h3>Storage</h3>
                            <div class="explore">Explore Collection →</div>
                        </div>
                    </div>
                </div>
                <button class="carousel-nav-btn next" onclick="scrollCarousel('accCarousel', 300)">›</button>
            </div>
        </div>
    </section>

    <section class="looking-for">
        <div class="section-title fade-up">
            What are you looking for <span class="gradient">?</span>
        </div>
        <div class="looking-grid">
            <div class="tilt-card fade-left">
                <div class="badge badge-new">New</div>
                <img src="/mnt/agents/upload/image(9).png" alt="New Laptops">
                <div class="tilt-card-body">
                    <h3>New Laptops</h3>
                    <div class="sub">Original sealed packaging</div>
                    <p>Brand new laptops, latest generation, ideal for work, studies or gaming. Performance and reliability guaranteed.</p>
                    <a href="#" class="link">
                        View models →
                        <span class="arrow-circle">→</span>
                    </a>
                </div>
            </div>
            <div class="tilt-card fade-right">
                <div class="badge badge-refurb">Refurbished</div>
                <img src="https://images.unsplash.com/photo-1496181133206-80ce9b88a853?w=600&q=80" alt="Pre-Owned">
                <div class="tilt-card-body">
                    <h3>Certified Pre-Owned</h3>
                    <div class="sub">Certified & guaranteed by ROC DZ</div>
                    <p>Pre-owned laptops in excellent and good condition, tested and verified by our experts. Performance at the best price with ROC DZ guarantee.</p>
                    <a href="#" class="link">
                        View models →
                        <span class="arrow-circle">→</span>
                    </a>
                </div>
            </div>
        </div>
    </section>

    <section class="pick-day">
        <div class="pick-container">
            <div class="pick-title-stack fade-left">
                <div class="label">Product of the day — Ref: ROC-2026</div>
                <h2>
                    <span class="white">THE</span><br>
                    <span class="gradient">PICK</span><br>
                    <span class="white">OF THE</span><br>
                    <span class="gradient">DAY</span>
                </h2>
                <div class="pick-specs">
                    <div class="spec-row">
                        <span class="label">Processor</span>
                        <span class="value">Intel Core i5-13420H Min:2.1GHz/ Max:4.6GHz (12 CPUs) 8 Cœurs / 12 Threads</span>
                    </div>
                    <div class="spec-row">
                        <span class="label">Graphics</span>
                        <span class="value">NVIDIA GeForce RTX 3050 with 6GB DDR6 / Intel UHD Graphics</span>
                    </div>
                    <div class="spec-row">
                        <span class="label">Memory</span>
                        <span class="value">16GB DDR4</span>
                    </div>
                </div>
            </div>

            <div class="pick-image scale-in">
                <img src="https://images.unsplash.com/photo-1588872657578-7efd1f1555ed?w=600&q=80" alt="HP Victus">
            </div>

            <div class="pick-info fade-right">
                <div class="status-badge">SYSTEM_STATUS: OPTIMAL</div>
                <div class="price-label">Current Value</div>
                <div class="price">179500 <span>DA</span></div>
                <div class="stock">Out of Stock</div>
                <a href="#" class="btn-product">
                    View Product →
                </a>
                <div class="pick-trust">
                    <p>// AUTHENTIFIÉ ROC DZ</p>
                    <p>// GARANTIE 6 MOIS</p>
                    <p>// LIVRAISON EXPRESS & SÉCURISÉE</p>
                </div>
            </div>
        </div>
    </section>

    <section class="why-us" id="why-us">
        <div class="glass-container fade-up">
            <div class="glass-header">
                <div class="left">
                    <div class="label"><div class="line"></div>Our Commitments</div>
                    <h2>Why choose<br><span class="gradient">us?</span></h2>
                </div>
                <div class="right">
                    Our selection of the best products, chosen for their quality and exceptional value
                </div>
            </div>
            <div class="glass-grid">
                <div class="glass-card">
                    <div class="bg-num">01</div>
                    <div class="icons">
                        <svg fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="1.5" d="M9 3v2m6-2v2M9 19v2m6-2v2M5 9H3m2 6H3m18-6h-2m2 6h-2M7 19h10a2 2 0 002-2V7a2 2 0 00-2-2H7a2 2 0 00-2 2v10a2 2 0 002 2zM9 9h6v6H9V9z"/></svg>
                        <svg fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="1.5" d="M9 12l2 2 4-4m6 2a9 9 0 11-18 0 9 9 0 0118 0z"/></svg>
                    </div>
                    <h3>Tested & Certified PCs</h3>
                    <p>Every computer is checked and tested to guarantee performance and reliability.</p>
                </div>
                <div class="glass-card">
                    <div class="bg-num">02</div>
                    <div class="icons">
                        <svg fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="1.5" d="M9 12h6m-6 4h6m2 5H7a2 2 0 01-2-2V5a2 2 0 012-2h5.586a1 1 0 01.707.293l5.414 5.414a1 1 0 01.293.707V19a2 2 0 01-2 2z"/></svg>
                        <svg fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="1.5" d="M9 12l2 2 4-4m5.618-4.016A11.955 11.955 0 0112 2.944a11.955 11.955 0 01-8.618 3.04A12.02 12.02 0 003 9c0 5.591 3.824 10.29 9 11.622 5.176-1.332 9-6.03 9-11.622 0-1.042-.133-2.052-.382-3.016z"/></svg>
                    </div>
                    <h3>ROC DZ Guarantee</h3>
                    <p>Buy with confidence with a warranty on our products.</p>
                </div>
                <div class="glass-card">
                    <div class="bg-num">03</div>
                    <div class="icons">
                        <svg fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="1.5" d="M12 8c-1.657 0-3 .895-3 2s1.343 2 3 2 3 .895 3 2-1.343 2-3 2m0-8c1.11 0 2.08.402 2.599 1M12 8V7m0 1v8m0 0v1m0-1c-1.11 0-2.08-.402-2.599-1M21 12a9 9 0 11-18 0 9 9 0 0118 0z"/></svg>
                        <svg fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="1.5" d="M5 13l4 4L19 7"/></svg>
                    </div>
                    <h3>Best value for money</h3>
                    <p>New and pre-owned PCs selected to offer the best performance at the best price.</p>
                </div>
                <div class="glass-card">
                    <div class="bg-num">04</div>
                    <div class="icons">
                        <svg fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="1.5" d="M17.657 16.657L13.414 20.9a1.998 1.998 0 01-2.827 0l-4.244-4.243a8 8 0 1111.314 0z"/><path stroke-linecap="round" stroke-linejoin="round" stroke-width="1.5" d="M15 11a3 3 0 11-6 0 3 3 0 016 0z"/></svg>
                        <svg fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="1.5" d="M13 16h-1v-4h-1m1-4h.01M21 12a9 9 0 11-18 0 9 9 0 0118 0z"/></svg>
                    </div>
                    <h3>Delivery to all 69 wilayas</h3>
                    <p>Receive your order anywhere in Algeria quickly and safely.</p>
                </div>
            </div>
        </div>
    </section>

    <button class="back-to-top" id="backToTop" onclick="window.scrollTo({top:0,behavior:'smooth'})">↑</button>

    <script>
        // ===== CANVAS ANIMATED BACKGROUND =====
        const canvas = document.getElementById('bg-canvas');
        const ctx = canvas.getContext('2d');
        let width, height;
        let particles = [];
        let waves = [];
        let mouse = { x: 0, y: 0 };

        function resize() {
            width = canvas.width = window.innerWidth;
            height = canvas.height = window.innerHeight;
        }
        resize();
        window.addEventListener('resize', resize);

        for (let i = 0; i < 80; i++) {
            particles.push({
                x: Math.random() * width,
                y: Math.random() * height,
                size: Math.random() * 2 + 0.5,
                speedX: (Math.random() - 0.5) * 0.3,
                speedY: (Math.random() - 0.5) * 0.3,
                opacity: Math.random() * 0.5 + 0.2
            });
        }

        for (let i = 0; i < 3; i++) {
            waves.push({
                y: height * 0.3 + i * 150,
                amplitude: 50 + i * 20,
                frequency: 0.002 + i * 0.001,
                speed: 0.01 + i * 0.005,
                phase: i * Math.PI / 3,
                color: i === 0 ? 'rgba(233, 30, 140, 0.12)' : i === 1 ? 'rgba(184, 41, 221, 0.08)' : 'rgba(255, 255, 255, 0.04)'
            });
        }

        function drawWave(wave, time) {
            ctx.beginPath();
            ctx.moveTo(0, wave.y);
            for (let x = 0; x < width; x += 2) {
                const y = wave.y + Math.sin(x * wave.frequency + time * wave.speed + wave.phase) * wave.amplitude;
                ctx.lineTo(x, y);
            }
            ctx.strokeStyle = wave.color;
            ctx.lineWidth = 1.5;
            ctx.stroke();
        }

        function animate(time) {
            ctx.clearRect(0, 0, width, height);

            waves.forEach(wave => drawWave(wave, time * 0.001));

            particles.forEach(p => {
                p.x += p.speedX;
                p.y += p.speedY;

                if (p.x < 0) p.x = width;
                if (p.x > width) p.x = 0;
                if (p.y < 0) p.y = height;
                if (p.y > height) p.y = 0;

                const dx = mouse.x - p.x;
                const dy = mouse.y - p.y;
                const dist = Math.sqrt(dx * dx + dy * dy);
                if (dist < 150) {
                    p.x -= dx * 0.01;
                    p.y -= dy * 0.01;
                }

                ctx.beginPath();
                ctx.arc(p.x, p.y, p.size, 0, Math.PI * 2);
                ctx.fillStyle = 'rgba(233, 30, 140, ' + p.opacity + ')';
                ctx.fill();
            });

            particles.forEach((p1, i) => {
                particles.slice(i + 1).forEach(p2 => {
                    const dx = p1.x - p2.x;
                    const dy = p1.y - p2.y;
                    const dist = Math.sqrt(dx * dx + dy * dy);
                    if (dist < 120) {
                        ctx.beginPath();
                        ctx.moveTo(p1.x, p1.y);
                        ctx.lineTo(p2.x, p2.y);
                        ctx.strokeStyle = 'rgba(184, 41, 221, ' + (1 - dist / 120) * 0.15 + ')';
                        ctx.lineWidth = 0.5;
                        ctx.stroke();
                    }
                });
            });

            requestAnimationFrame(animate);
        }

        window.addEventListener('mousemove', e => {
            mouse.x = e.clientX;
            mouse.y = e.clientY;
        });

        requestAnimationFrame(animate);

        // ===== CAROUSEL NAVIGATION =====
        function scrollCarousel(id, amount) {
            document.getElementById(id).scrollBy({ left: amount, behavior: 'smooth' });
        }

        // ===== SCROLL ANIMATIONS =====
        const observerOptions = {
            threshold: 0.1,
            rootMargin: '0px 0px -50px 0px'
        };

        const observer = new IntersectionObserver((entries) => {
            entries.forEach(entry => {
                if (entry.isIntersecting) {
                    entry.target.classList.add('visible');
                }
            });
        }, observerOptions);

        document.querySelectorAll('.fade-up, .fade-left, .fade-right, .scale-in').forEach(el => observer.observe(el));

        // ===== BACK TO TOP =====
        const backToTop = document.getElementById('backToTop');
        window.addEventListener('scroll', () => {
            if (window.pageYOffset > 500) {
                backToTop.classList.add('visible');
            } else {
                backToTop.classList.remove('visible');
            }
        });

        // ===== SMOOTH SCROLL =====
        document.querySelectorAll('a[href^="#"]').forEach(anchor => {
            anchor.addEventListener('click', function(e) {
                e.preventDefault();
                const target = document.querySelector(this.getAttribute('href'));
                if (target) {
                    target.scrollIntoView({ behavior: 'smooth', block: 'start' });
                }
            });
        });
    </script>
</body>
</html>